import SitePageTemplate from "@/components/core/SitePageTemplate";

export default function AboutPage() {
  return (
    <SitePageTemplate
      slug="about"
      fallbackTitle="About Rooted Kenya"
      fallbackSummary="Who we are, what we believe, and how we curate experiences across Kenya."
    />
  );
}
