import SitePageTemplate from "@/components/core/SitePageTemplate";

export default function TermsOfServicePage() {
  return (
    <SitePageTemplate
      slug="terms-of-service"
      fallbackTitle="Terms of Service"
      fallbackSummary="The terms that govern use of Rooted Kenya services and content."
    />
  );
}
