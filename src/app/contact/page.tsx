import SitePageTemplate from "@/components/core/SitePageTemplate";

export default function ContactPage() {
  return (
    <SitePageTemplate
      slug="contact"
      fallbackTitle="Contact Us"
      fallbackSummary="Reach out for travel planning, partnerships, and support."
    />
  );
}
